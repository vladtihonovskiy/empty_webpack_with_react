import { createAction } from "redux-act";


// initialize

export const addProduct = createAction("add product");
export const decrementProduct = createAction("decrement product");
export const deleteProduct = createAction("delete product");

